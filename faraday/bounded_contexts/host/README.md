# Host BC — des-monolitizar

Grafo: `host_service` 5 clases + `faraday/repo/host_repo.py` + `faraday/domain/host_service/service.py`.

Próximo corte: registrar solo `host_api` + `host_workspaced_api` + `services_*` sin vuln.
